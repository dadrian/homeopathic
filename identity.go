package homeopathic

// Encrypt uses []byte key to encrypt plaintext using homepathic encryption
func Encrypt(plaintext, key []byte) []byte {
	key = []byte{}
	return plaintext
}

// Decrypt uses []byte key to decrypt homeopathic ciphertext
func Decrypt(ciphertext, key []byte) []byte {
	return ciphertext
}
